services:
  svc_tsdb:
    command: postgres
      -c config_file=/etc/postgresql/postgresql.conf
      -c hba_file=/etc/postgresql/pg_hba.conf
    container_name: timescaledb
    healthcheck:
      test: pg_isready -d db_data
      interval: 30s
      timeout: 60s
      retries: 5
      start_period: 80s
    hostname: timescaledb
    image: timescale/timescaledb:2.23.0-pg18
    networks:
      - network_internal
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - PGDATA=/pgdata
    ports:
      - "5432:5432"
    # user: postgres
    volumes:
      - ../../src/doc/external_services/tsdb/postgresql.conf:/etc/postgresql/postgresql.conf
      - ../../src/doc/external_services/tsdb/pg_hba.conf:/etc/postgresql/pg_hba.conf
      - ../../src/doc/external_services/tsdb/init.sql:/docker-entrypoint-initdb.d/init.sql
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - timescaledb_volume:/pgdata

  pgadmin:
    container_name: pgadmin
    environment:
      - PGADMIN_DEFAULT_EMAIL=test@test.com
      - PGADMIN_DEFAULT_PASSWORD=123
    hostname: pgadmin
    image: dpage/pgadmin4:latest
    ports:
      - "8000:80"
    networks:
      - network_internal
    volumes:
      - pgadmin_volume:/var/lib/pgadmin

networks:
  network_internal:

volumes:
  pgadmin_volume:
  timescaledb_volume:
